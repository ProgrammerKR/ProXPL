# Your First Program

Create a file named `hello.prox`:

```javascript
func main() {
    print("Welcome to ProXPL!");
    let name = input("What is your name? ");
    print("Hello, " + name + "!");
}

main();
```

Run it:
```bash
prm run hello.prox
```

---

**Status**: Stable
