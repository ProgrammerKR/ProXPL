// fib.prox
func fib(n) {
    if (n <= 1) return n;
    return fib(n - 1) + fib(n - 2);
}

func main() {
    let n = 10;
    print("Fibonacci of " + to_string(n) + " is: " + to_string(fib(n)));
}

main();
