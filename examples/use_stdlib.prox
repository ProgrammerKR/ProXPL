// Demo of standard library modules (simulating potential stdlib structure)
// Currently 'utils/io' is mocked in PRM but theoretically could be in stdlib

// use std/math; // Not implemented in current file structure
// For now, let's use the PRM mock 'math/advanced'

use math/advanced;

func main() {
    print("Standard Library Demo (Simulated)");
    
    let res = cube(3);
    print("Cube of 3 is: " + res);
}

main();
